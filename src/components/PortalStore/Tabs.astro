---

---
<div class="tabs">
  <button class="tab active" data-tab="vender">REALIZAR UNA VENTA</button>
  <button class="tab color1" data-tab="pagos">REALIZAR PAGOS</button>
  <button class="tab" data-tab="venderproducto">VENDER PRODUCTOS</button>
  <button class="tab color2" data-tab="ventasPorFecha">REVISAR VENTAS DEL DIA</button>
  <button class="tab colorCre" data-tab="cliente">CREAR CLIENTE</button>
  
</div>

<script>
  const tabs = document.querySelectorAll('.tab');
  const panels = document.querySelectorAll('.tab-panel');

  tabs.forEach(tab => {
    tab.addEventListener('click', () => {
      const target = tab.dataset.tab;

      // activar tab
      tabs.forEach(t => t.classList.remove('active'));
      tab.classList.add('active');

      // mostrar panel
      panels.forEach(panel => {
        panel.classList.toggle(
          'active',
          panel.dataset.tab === target
        );
      });
    });
  });
</script>
